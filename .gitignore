# Secrets and environment
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Virtual environments
venv/
.venv/
env/

# Logs and runtime
logs/
*.log
storage/reports/

# Local databases / scratch
*.db
*.sqlite
*.sqlite3

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build artifacts
dist/
build/

# Claude Code local settings
.claude/settings.local.json

# Scratch / temp work dirs
backend/tmp/

# Graphify output and intermediates
graphify-out/
.graphify_*

test-results-ui/

# Patch-script backups (any .bak variant: .bak, .bak2, .bak_audit, .bak5v2, ...)
*.bak*

# Phase scratch / backup snapshots (never commit — recoverable from history)
_backup/
_hotfix/
_smoke*/
_phase*/

# One-off dev scripts kept out of the repo root
/*.ps1
/apply_*.py
/fix_*.py
/probe_*.py
/cleanup_*.sql
*.broken_*
fix-writable-stale.sql
restart_UI_5174.txt

# Lifecycle evidence logs
scripts/lifecycle/evidence_full*.txt

# OPC web working snapshots (scratch duplicates)
opc_web_*/

# Generated report preview HTML (output of smoke scripts; the .py generators are tracked)
*.preview.html


*.bak_*







scripts/ui_smoke_fail.png
*.db-shm
*.db-wal
*.db-journal
storage/sf_buffer.db*

# local installer prerequisites - do not commit
/Installer_Pre-requisite/

# local installers - never commit


# Scratch dirs & one-off artifacts (repo hygiene pass)
# all top-level scratch dirs use a leading underscore
/_*/
# database dumps never belong in the repo
*.dump
# scratch mermaid diagram exports
*Mermaid*.pdf
# generated report preview HTML
*_report_preview.html
# one-off root math/test scripts
/test_*.mjs
