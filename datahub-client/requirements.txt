# Pinned versions for reproducible installs. The looser bounds in
# pyproject.toml let pip-tools / poetry users resolve fresh; this
# file is for "I want exactly what we tested with."
#
# Regenerate (PowerShell):
#   pip-compile pyproject.toml -o requirements.txt

PySide6==6.7.0
pydantic==2.7.1
httpx==0.27.0
tenacity==8.5.0
asyncua==1.1.5

# Windows-only — pywin32 ships as a wheel.
pywin32==306; sys_platform == "win32"
