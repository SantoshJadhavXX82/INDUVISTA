# Phase 0 dependencies. Pinned for reproducibility.
# Phase 1 will add pymodbus; later phases will add anyio test helpers,
# prometheus-client, etc.

fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
alembic==1.13.3
psycopg2-binary==2.9.10
pydantic==2.10.0
pydantic-settings==2.6.0
pymodbus==3.7.4
redis==5.0.8
psutil==6.1.0

# Phase 14.11 - bulk import for alarm rules.
# openpyxl is pure-Python with no compiled deps. python-multipart is
# required by FastAPI for parsing multipart uploads (file fields).
openpyxl==3.1.5
python-multipart==0.0.20

# Phase OPC-web.2 — asyncua client for the opc_supervisor worker.
# 1.1.8 verified working on Python 3.11/3.12; broken on 3.14 (PEP 749
# annotation deferral collides with asyncua's dataclass introspection).
# Backend image is Python 3.12 so we're fine.
asyncua>=1.1,<2

# Phase 21 - auth (users + RBAC + JWT sessions).
# pyjwt: stateless session tokens (HS256). passlib[bcrypt]: bcrypt password
# hashing for local users (slow-hash, appropriate for low-entropy passwords).
pyjwt==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# Phase 20 - PDF report generation (pure-Python, no system deps).
reportlab==4.2.5
